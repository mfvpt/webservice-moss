# Configuration properties template
#
# values starting and ending with @ can be replaced during project build file execution

# database access debug mode (if true, prints optional messages)
db.debug=false

# database JDBC driver name
db.driver=com.microsoft.sqlserver.jdbc.SQLServerDriver

# database server and port
db.server=SRV-003-004.posi.local
db.port=1433

# database name
db.name=ISICodigosPostais

# database integrated security option and username and password
db.integrated-security=false
db.username=G00
db.password=G00
